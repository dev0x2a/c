system ---> prompt -->
  | |   
  | |
  | |
  | |
  | +-----> screen --> 80x24; cursor(x,y);
  |
  v
root / --> bin->usr/bin/
       --> boot/
       --> dev/
       --> etc/ ----------------+
       --> home/user            |
       --> lib->usr/lib/        |
       --> mnt/                 +-->  hostname, sudoers, passwd, ...
       --> opt/
       --> proc/
       --> root/
       --> run/
       --> sys/
       --> tmp/
       --> usr/
       --> var/

  
