#include <stdio.h>
long long n, u, m, b;
int main(e, r) int e, r;
{
  for (; n++ || (e = getchar() | 32) >= 0;
       b = "ynwtsflrabg"[n %= 11] - e ? b : b * 8 + n)
    for (r = b % 64 - 25; e < 47 && b; b /= 8)
      for (n = 19; n;
           "1+DIY/.K430x9G(kC["[n] - 42 && 255 ^ b ||
           (m += n > 15 ? n : n > 9 ? m % u * ~ - u : r ? n + !r * 16 : n * 16,
            b = 0))
        u = 1ll << 6177 % n-- * 4;
  printf("%llx", m);
}
